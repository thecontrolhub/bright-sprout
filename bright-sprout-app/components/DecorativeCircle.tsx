import { YStack, styled } from 'tamagui';

export const DecorativeCircle = styled(YStack, {
  name: 'DecorativeCircle',
  position: 'absolute',
  // Default props
  opacity: 0.1,
  width: 100,
  height: 100,
});